import { MapPin, Plus } from 'lucide-react'
import type { Property } from '../types/property'
import { SearchBox } from '../components/common/SearchBox'
export function PropertyMapView({ query, onQueryChange, rows, selected, onSelect }: { query: string; onQueryChange: (value: string) => void; rows: Property[]; selected: Property; onSelect: (property: Property) => void }) {
 return <><div className="title-row"><div><p className="eyebrow">GIS PROPERTY DIRECTORY</p><h1>Property Map</h1><p className="subhead">Locate, filter, and review registered properties by geographic area.</p></div><button className="primary"><Plus size={18}/> Update location</button></div><div className="map-layout"><section className="card map-panel"><div className="map-tools"><SearchBox value={query} onChange={onQueryChange}/><select><option>All barangays</option><option>Barangay San Isidro</option></select><select><option>All classifications</option><option>Residential</option></select></div><div className="map-canvas"><div className="map-label north">N</div><div className="road road-one"/><div className="road road-two"/><div className="water"/><span className="place place-one">San Isidro</span><span className="place place-two">Poblacion</span>{rows.map(p => <button aria-label={p.id} onClick={() => onSelect(p)} className={selected.id === p.id ? 'marker selected' : 'marker'} style={{left:`${p.x}%`,top:`${p.y}%`,background:p.color}} key={p.id}><MapPin size={20}/></button>)}<div className="map-credit">Map preview · OpenStreetMap-ready</div></div><div className="map-legend"><span><i className="res"/>Residential</span><span><i className="com"/>Commercial</span><span><i className="agri"/>Agricultural</span><span>{rows.length} locations shown</span></div></section><PropertyDetails property={selected}/></div></>
}
function PropertyDetails({ property }: { property: Property }) { return <aside className="card map-detail"><p className="eyebrow">SELECTED PROPERTY</p><h2>{property.id}</h2><p className="owner">{property.owner}</p><div className="detail-row"><MapPin/><span>{property.location}</span></div><div className="detail-grid"><div><span>Property type</span><strong>{property.type}</strong></div><div><span>GPS coordinates</span><strong>14.5995, 120.9842</strong></div><div><span>Market value</span><strong>{property.market}</strong></div><div><span>Assessed value</span><strong>{property.assessed}</strong></div></div><button className="primary full">View complete record</button><button className="outline">Get directions</button></aside> }
